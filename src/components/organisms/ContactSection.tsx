import * as React from 'react'
import SectionTitle from '../atomic/SectionTitle'

const ContactSection: React.FC = () => {
  return (
    <div>
      <SectionTitle text="CONTACT" textAlign="center" />
    </div>
  )
}

export default ContactSection
